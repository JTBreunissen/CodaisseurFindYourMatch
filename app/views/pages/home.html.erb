<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>Co-opdaisseur</title>

    <link href="../../assets/stylesheets/jumbotron.scss" rel="stylesheet">

  </head>

  <body>
    <div class="jumbotron">
      <div class="container">
        <% if !user_signed_in? %>
        <h1>Welcome to Co-opdaisseur! </h1>
        <p>Find out who you are paired up with today, and see who you worked with before, by logging in!</p>
        <p><%= link_to "Log In", new_user_session_path, class: "btn btn-primary btn-lg" %></p>
        <% else %>
        <h1>Welcome to co-opdaisseur! </h1>
          <% if current_user.is_admin? %>
            <p>Welcome <%= current_user.name %>
            <p><%= link_to "View and Create Matches", admin_matches_path, class: "btn btn-primary btn-lg" %></p>
            <p><%= link_to "View and Create Courses", admin_courses_path, class: "btn btn-primary btn-lg" %></p>
          <% else %>
            <h3><%= current_user.name %> </h3>
            <p>click the link below to see your matches.
            <p><%= link_to "Matches", matches_path, class: "btn btn-primary btn-lg" %></p>
          <% end %>
        <% end %>
      </div>
    </div>
  </body>
</html>
